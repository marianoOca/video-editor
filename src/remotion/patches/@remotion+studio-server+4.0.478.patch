diff --git a/node_modules/@remotion/studio-server/dist/preview-server/routes/apply-codemod.js b/node_modules/@remotion/studio-server/dist/preview-server/routes/apply-codemod.js
index ec82965..2dd9070 100644
--- a/node_modules/@remotion/studio-server/dist/preview-server/routes/apply-codemod.js
+++ b/node_modules/@remotion/studio-server/dist/preview-server/routes/apply-codemod.js
@@ -64,6 +64,113 @@ const getCodemodUndoDescription = (codemod) => {
 const applyCodemodHandler = async ({ input: { codemod, dryRun, symbolicatedStack }, logLevel, remotionRoot, entryPoint, }) => {
     var _a;
     try {
+        // --- video-editor patch: native "Delete composition" → wipe project files ---
+        // Remotion's recast codemod can't delete a composition rendered via .map()
+        // (id={p.name} is not a string literal), and even when it works it only edits
+        // source, orphaning the project's files. Bypass it: delegate to the local
+        // sidecar (port 9848), which runs the Python delete_project (data + snapshot +
+        // public + state). Root.tsx discovers projects via require.context, so the
+        // composition drops from the sidebar on the next recompile.
+        if (codemod.type === 'delete-composition') {
+            const SIDECAR = 'http://127.0.0.1:9848';
+            if (dryRun) {
+                // Preview: enable the Delete button only if the sidecar is reachable.
+                try {
+                    const h = await fetch(`${SIDECAR}/health`);
+                    if (!h.ok) {
+                        throw new Error('bad health');
+                    }
+                }
+                catch (e) {
+                    return {
+                        success: false,
+                        reason: 'Delete needs the sidecar (port 9848). Start Studio with `npm run dev`.',
+                    };
+                }
+                return { success: true, diff: { additions: 0, deletions: 1 } };
+            }
+            // Real run: throw on failure so callApi rejects and the UI surfaces it
+            // (CodemodFooter.trigger reports success unless the promise rejects).
+            const res = await fetch(`${SIDECAR}/delete-project`, {
+                method: 'POST',
+                headers: { 'content-type': 'application/json' },
+                body: JSON.stringify({
+                    project: codemod.idToDelete,
+                    deleteInput: !!codemod.deleteInput,
+                    deleteOutput: !!codemod.deleteOutput,
+                }),
+            });
+            if (!res.ok) {
+                const data = await res.json().catch(() => ({}));
+                throw new Error((data && data.error) || `sidecar returned ${res.status}`);
+            }
+            return { success: true, diff: { additions: 0, deletions: 1 } };
+        }
+        // --- video-editor patch: native "Duplicate composition" → copy project files ---
+        // Same root cause as Delete: recast can't match id={p.name} (.map()'d
+        // composition), so bypass it and copy the project's three folders via the
+        // sidecar's duplicate_project (snapshot name + videoSrc rewritten to newId).
+        // The new snapshot json makes the copy appear in the sidebar on recompile.
+        if (codemod.type === 'duplicate-composition') {
+            const SIDECAR = 'http://127.0.0.1:9848';
+            if (dryRun) {
+                try {
+                    const h = await fetch(`${SIDECAR}/health`);
+                    if (!h.ok) {
+                        throw new Error('bad health');
+                    }
+                }
+                catch (e) {
+                    return {
+                        success: false,
+                        reason: 'Duplicate needs the sidecar (port 9848). Start Studio with `npm run dev`.',
+                    };
+                }
+                return { success: true, diff: { additions: 1, deletions: 0 } };
+            }
+            const res = await fetch(`${SIDECAR}/duplicate-project`, {
+                method: 'POST',
+                headers: { 'content-type': 'application/json' },
+                body: JSON.stringify({ from: codemod.idToDuplicate, to: codemod.newId }),
+            });
+            if (!res.ok) {
+                const data = await res.json().catch(() => ({}));
+                throw new Error((data && data.error) || `sidecar returned ${res.status}`);
+            }
+            return { success: true, diff: { additions: 1, deletions: 0 } };
+        }
+        // --- video-editor patch: native "Rename composition" → move project files ---
+        // Same bypass: rename_project moves the three folders + rewrites/moves the
+        // snapshot (and follows the active-project state file).
+        if (codemod.type === 'rename-composition') {
+            const SIDECAR = 'http://127.0.0.1:9848';
+            if (dryRun) {
+                try {
+                    const h = await fetch(`${SIDECAR}/health`);
+                    if (!h.ok) {
+                        throw new Error('bad health');
+                    }
+                }
+                catch (e) {
+                    return {
+                        success: false,
+                        reason: 'Rename needs the sidecar (port 9848). Start Studio with `npm run dev`.',
+                    };
+                }
+                return { success: true, diff: { additions: 1, deletions: 1 } };
+            }
+            const res = await fetch(`${SIDECAR}/rename-project`, {
+                method: 'POST',
+                headers: { 'content-type': 'application/json' },
+                body: JSON.stringify({ from: codemod.idToRename, to: codemod.newId }),
+            });
+            if (!res.ok) {
+                const data = await res.json().catch(() => ({}));
+                throw new Error((data && data.error) || `sidecar returned ${res.status}`);
+            }
+            return { success: true, diff: { additions: 1, deletions: 1 } };
+        }
+        // --- end video-editor patch ---
         const time = Date.now();
         const filePath = symbolicatedStack
             ? (0, apply_codemod_to_file_1.resolveFilePathFromSymbolicatedStack)(remotionRoot, symbolicatedStack)
diff --git a/node_modules/@remotion/studio-server/dist/preview-server/routes/open-in-file-explorer.js b/node_modules/@remotion/studio-server/dist/preview-server/routes/open-in-file-explorer.js
index a41abc3..3952f15 100644
--- a/node_modules/@remotion/studio-server/dist/preview-server/routes/open-in-file-explorer.js
+++ b/node_modules/@remotion/studio-server/dist/preview-server/routes/open-in-file-explorer.js
@@ -1,8 +1,15 @@
 "use strict";
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.handleOpenInFileExplorer = void 0;
+const node_path_1 = require("node:path");
 const open_directory_in_finder_1 = require("../../open-directory-in-finder");
 const handleOpenInFileExplorer = ({ input: { directory }, remotionRoot }) => {
-    return (0, open_directory_in_finder_1.openDirectoryInFinder)(directory, remotionRoot);
+    // video-editor patch: renders are written to <repo>/output, two levels above
+    // remotionRoot (= src/remotion), so they sit outside remotionRoot and the default
+    // guard rejects them ("Not allowed to open ../../output/..."). Widen the allowed
+    // root to the repo root so the render tab's "reveal in Finder" button works, while
+    // still bounding opens to inside the project.
+    const allowedRoot = node_path_1.resolve(remotionRoot, "..", "..");
+    return (0, open_directory_in_finder_1.openDirectoryInFinder)(directory, allowedRoot);
 };
 exports.handleOpenInFileExplorer = handleOpenInFileExplorer;
